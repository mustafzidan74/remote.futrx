// MessageSkeleton — transcript outline shown while a chat's events load.
import { MessageSkeleton } from "remote.futrx-web";

export const Default = () => (
  <div className="w-full max-w-2xl px-5 py-6">
    <MessageSkeleton />
  </div>
);
