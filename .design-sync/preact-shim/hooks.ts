// Preact→React shim for preact/hooks — identical signatures in React.
export {
  useState,
  useEffect,
  useLayoutEffect,
  useMemo,
  useCallback,
  useRef,
  useContext,
  useReducer,
  useId,
  useImperativeHandle,
} from "react";
