/**
 * Time units in milliseconds.
 *
 * A day was spelled out independently in the search filters, the search
 * ranking window, the usage range service and two tests — as both
 * `86_400_000` and `24 * 60 * 60 * 1000`, so a reader had to check they agreed.
 */

export const DAY_MS = 86_400_000;
