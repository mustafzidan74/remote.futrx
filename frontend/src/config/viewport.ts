/** Minimum visual-viewport coverage that is treated as an on-screen keyboard. */
export const KEYBOARD_MIN_COVERAGE_PX = 120;

/** Lets focus settle before restoring the layout viewport after an input blur. */
export const VIEWPORT_FOCUS_SETTLE_DELAY_MS = 120;
